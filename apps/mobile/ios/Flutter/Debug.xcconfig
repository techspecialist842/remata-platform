#include "Generated.xcconfig"
// Clave de Google Maps, si está configurada. El signo de interrogación
// hace que la inclusión sea opcional: sin el archivo, la compilación
// sigue y la app arranca sin mapa.
#include? "Mapas.xcconfig"
