# Copy to terraform.tfvars and fill in. Do not commit terraform.tfvars.

aws_region      = "us-east-1" # BLOCKED: must match bootstrap's region — pending client decision
container_image = ""          # BLOCKED: <ecr_repo_url>:staging-<sha>, only exists after first CI image push
alert_email     = ""          # BLOCKED: pending ops stakeholder
